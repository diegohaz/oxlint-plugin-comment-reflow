const element = (
  <div>
    {/* This comment describes the button and contains enough prose to wrap across several lines. */}
    <Button
      // These short
      // lines explain the next prop and form one paragraph with useful text.
      prop="value"
      /* This block describes the next prop and contains enough prose to wrap across several lines. */
      disabled
    />
    {/*
     * These short
     * lines form another paragraph with useful text.
     */}
  </div>
);
